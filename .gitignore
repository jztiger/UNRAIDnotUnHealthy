.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Local dev artifacts
data/
volumes/
*.log

# Build artifacts
dist/
build/

# docker-compose.override.yml is auto-merged with docker-compose.yml by
# `docker compose` — keep host-specific tweaks (static LAN IP, macvlan name)
# here so `git pull` never conflicts.
docker-compose.override.yml
docker-compose.override.yaml

# .env holds API keys for the *arr exporters. Never commit it.
.env

# Per-host deploy config + secrets used by scripts/deploy-to-unraid.sh.
scripts/.deploy.env
