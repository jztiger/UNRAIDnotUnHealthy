auth_enabled: false

server:
  http_listen_address: 127.0.0.1
  http_listen_port: 3100
  grpc_listen_address: 127.0.0.1
  grpc_listen_port: 9096
  log_level: warn

common:
  # Keep all internal traffic on loopback. Without this, Loki's lifecycler
  # auto-discovers the container's primary IP — which on macvlan is the
  # external LAN IP (192.168.1.100) — and advertises that in the ring. The
  # container can't loopback to its own macvlan IP from inside, so any push
  # routed via the ring fails with "connection refused."
  instance_addr: 127.0.0.1
  path_prefix: /var/lib/loki
  storage:
    filesystem:
      chunks_directory: /var/lib/loki/chunks
      rules_directory: /var/lib/loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  retention_period: ${LOKI_RETENTION:-14d}
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  # Don't reject old samples on ingest — Alloy replays each running
  # container's full historical Docker log on first connection, and some
  # containers haven't restarted in months. Anything outside the
  # retention window will be compacted away on its own schedule.
  reject_old_samples: false
  max_query_series: 5000
  allow_structured_metadata: true

compactor:
  working_directory: /var/lib/loki/compactor
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
  retention_delete_worker_count: 150
  delete_request_store: filesystem

ingester:
  # Per-stream out-of-order acceptance window. When Alloy backfills a
  # long-running container's Docker log on first connection, those entries
  # may be days or weeks old relative to the stream watermark. Default
  # max_chunk_age=2h rejects them with "entry too far behind" 400s.
  # Aligned with Alloy's drop_old=2160h (90d) so anything Alloy forwards
  # gets accepted; chunks still flush after chunk_idle_period when idle.
  max_chunk_age: 2160h
  chunk_idle_period: 1h

ruler:
  storage:
    type: local
    local:
      directory: /var/lib/loki/rules
  rule_path: /var/lib/loki/rules-temp
  ring:
    kvstore:
      store: inmemory

analytics:
  reporting_enabled: false
